# CLAUDE.md

本文件为 Claude Code（claude.ai/code）处理本仓库代码时提供指引。

## 仓库概览

这是一个名为 "batch-call-runner" 的新仓库，目前似乎处于早期开发阶段。仓库当前结构非常简单——只有 README.md 文件和 git 配置。

## Git 信息

- **远程 Origin**: `git@github.com:cssdao/batch-call-runner.git`
- **主分支**: `main`
- **当前状态**: 初始提交（a0aed34）—— 仓库刚完成初始化

## 开发命令

由于本仓库处于初始状态，尚无 package.json、依赖或源代码，因此标准开发命令暂不可用。

后续开发可能需要：
- 初始化 package.json（`npm init` 或等效命令）
- 搭建项目结构并安装依赖
- 添加构建、测试和 lint 脚本

## 项目结构

仓库当前结构非常简单：
```
batch-call-runner/
├── README.md          # 基本项目说明
└── .git/             # Git 配置
```

## 架构说明

本项目似乎与批量调用执行相关，但由于仓库仅包含初始 git 设置，实际架构和实现细节尚未确定。

## 后续开发步骤

处理本仓库时，你可能需要按以下步骤推进：
1. 使用合适的包管理器初始化项目
2. 根据目标技术栈搭建基础项目结构
3. 定义批量调用执行功能
4. 添加合适的配置文件、测试和文档
